#ifndef UTILS_H
#define UTILS_H

#include <stdio.h>
#include <string>
#include <vector>
#include "TDirectoryFile.h"

typedef std::vector<std::string> stringvec;
typedef std::vector<TDirectoryFile*> tdfvec;
//typedef ROOT::RDF::RInterface<ROOT::Detail::RDF::RJittedFilter, void> interface;
//typedef std::vector<interface> interfacevec;

#endif
